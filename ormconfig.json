{
  "type": "sqlite",
  "synchronize": true,
  "logging": false,
  "database": "./db.sqlite",
  "entities": ["src/entity/**/*.ts"],
  "migrations": ["src/migration/**/*.ts"],
  "cli": {
    "entitiesDir": "src/entity",
    "migrationsDir": "src/migration"
  }
}
